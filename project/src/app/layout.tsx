import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Mindful Journal - AI-Powered Journaling & Mood Tracking',
  description: 'Track your thoughts and emotions with intelligent insights. A modern journaling platform with mood tracking and AI-generated reflections.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
